{
  "name": "schedule-worker",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "link-shared": "rm -rf shared && ln -s ../shared ./shared",
    "dev": "npm run link-shared && wrangler dev --test-scheduled",
    "deploy": "npm run link-shared && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.47.0"
  }
}
